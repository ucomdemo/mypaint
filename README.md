# Paint Studio

這是一個可在本機執行的簡易繪圖網站應用程式，功能包含筆刷、橡皮擦、直線、矩形、圓形、顏色選擇、筆觸粗細、畫布尺寸調整、清除與 PNG 儲存。

## 使用方式

直接用瀏覽器開啟：

```powershell
start .\index.html
```

或啟動本機靜態伺服器：

```powershell
python -m http.server 8000
```

然後開啟：

```text
http://localhost:8000
```
